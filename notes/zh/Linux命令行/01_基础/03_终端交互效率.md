# ⌨️ 编辑器与终端交互 (Efficiency)

### 1. Vim 编辑器 (必学)
Vim 是 Linux 最核心的编辑器。初学者最困惑的是：**为什么我按键盘没反应？** 因为 Vim 默认处于 **普通模式**。

- **普通模式** (默认)：按键是命令。
  - **进入编辑的方式**：
    - `i`：在光标**当前位置**开始输入 (Insert)。
    - `a`：在光标**后面**开始输入 (Append)。
    - `o`：在当前行**下方**新开一行开始输入。
  - **核心操作**：
    - `Esc`：**回到普通模式** (不管你在干什么，先按两下 Esc 准没错)。
    - `x`：删除当前字符。
    - `dd`：删除当前行；`yy`：复制当前行；`p`：粘贴。
    - `u`：撤销上一部操作。
    - `/word`：搜索 "word"，按 `n` 查找下一个。
- **命令模式** (在普通模式下按 `:` )：
  - **`:q!`**：**强制退出且不保存** (救命指令)。
  - **`:wq`**：保存并退出。
  - `:set number`：显示行号。

---

### 2. Nano 编辑器 (极简)
Nano 比 Vim 更简单，操作提示直接显示在底部 (`^` 代表 `Ctrl` 键)：
- `Ctrl + O`：保存文件。
- `Ctrl + X`：退出。

---

### 3. 终端交互快捷键 (Terminal Shortcuts)

#### 文本编辑与进程控制
- **`Tab`**：**自动补全** (本教程最重要的快捷键)。
- **`Ctrl + A` / `Ctrl + E`**：跳转到行首/行尾。
- **`Ctrl + C`**：终止当前程序 (强行关掉)。
- **`Ctrl + Z`**：**挂起当前程序**。
  - **⚠️ 避坑**：新手常误按 `Ctrl+Z` 以为是撤销，结果程序只是“暂停”并藏到了后台。输入 **`fg`** 即可把它接回前台。
- **`Ctrl + L`**：清屏 (等同于 `clear`)。
- **`Ctrl + D`**：退出当前 Shell (等同于 `exit`)。

#### 历史记录操作 (History Expansion)
- `!!`：执行上一条命令 (如 `sudo !!`)。
- `!$`：引用上一条命令的**最后一个参数**。
- **`!^`**：引用上一条命令的**第一个参数**。
- **`!n`**：执行历史记录中第 **n** 条命令。
- **💡 效率组合：快捷键版**
  - **`Alt + .`**：快速粘贴上一条命令的最后一个参数 (比 `!$` 更快，可多次按切换)。
  - **`Ctrl + R`**：**搜索历史命令** (Reverse-i-search)。输入关键词即可匹配。推荐安装 `fzf` 替代默认搜索，体验飞升。
  - **`Ctrl + X, Ctrl + E`**：**在编辑器中编辑长命令**。当你写了五行长的命令发现第一行错了，按它，会在 Vim 里打开这行命令，改完保存自动执行。

---

### 4. 核心：I/O 重定向与管道 (Redirection & Pipes)

这是 Linux 哲学的精髓：**将一个命令的输出作为另一个命令的输入。**

#### 💡 理解三个“流”
1.  **stdin (0)**：标准输入（通常是键盘）。
2.  **stdout (1)**：标准输出（屏幕上的正常提示）。
3.  **stderr (2)**：标准错误（屏幕上的报错信息）。

#### 常用重定向符号
- **`>`**：覆盖写入。`ls > files.txt` 将列表存入文件。
- **`>>`**：追加写入。`echo "new line" >> log.txt`。
- **`2>`**：**仅重定向错误**。`find / -name "test" 2> /dev/null` (把烦人的 "Permission denied" 报错扔进黑洞)。
- **`2>&1`**：**合并输出**。将错误流 (2) 合并到标准输出流 (1) 中。
  - **💡 为什么有个 `&`？**：如果不加 `&`，系统会把 `1` 当成一个名为 `1` 的普通文件。加了 `&` 代表“引用文件描述符 1”。
- **`&>`**：(现代简写) 效果等同于 `2>&1`，将正确和错误的结果都存入同一个文件。
- **`|` (管道)**：**核心神技**。将左边命令的 `stdout` 直接喂给右边命令的 `stdin`。
  - *例子*：`cat access.log | grep "Error" | wc -l` (读文件 -> 过滤错误 -> 数行数)。

---

### 5. 别名管理 (Alias)
通过 `alias` 将长命令变短。

- **临时生效**：直接在终端输入 `alias ll='ls -lha'`。
- **永久生效**：
  1. 打开配置文件：`nano ~/.bashrc` (或 `~/.zshrc`)。
  2. 在文件末尾添加别名行。
  3. 保存退出并运行：**`source ~/.bashrc`** 使其立即生效。

> 💡 **选择恐惧症？修改哪个文件？**
> - **管自己**：修改 `~/.bashrc` (仅对当前用户生效)。
> - **管全家**：修改 `/etc/profile` (对所有用户生效，需 sudo)。
> **原则**：通常情况下，只管自己改 `.bashrc` 即可，更安全且不影响他人。

- `alias ll='ls -lha'`：列出所有文件的详细信息。
- `alias grep='grep --color=auto'`：让 grep 输出带颜色。
- `alias update='sudo apt update && sudo apt upgrade'`：一键更新系统。
- `alias ..='cd ..'`：快速返回上级目录。
