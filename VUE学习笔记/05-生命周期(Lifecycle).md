# 05. 生命周期 (Lifecycle) 🌸

Vue 组件从出生到死亡会经历几个阶段。

## 1. 创建阶段 (Creation)
- **setup()**: 组件实例被创建，Props 解析完毕。此时 DOM 还没生成。

## 2. 挂载阶段 (Mounting)
- **onBeforeMount**: 模板编译好了，但还没放到页面上。
- **onMounted**: **关键钩子**。
  - 真实 DOM 已经渲染完毕。
  - **此时可以做的事**：
    1. `document.getElementById` 获取节点。
    2. 发起 `fetch` / `axios` 请求获取数据。
    3. 启动定时器。

## 3. 更新阶段 (Updating)
当响应式数据 (Ref/Reactive) 变化时触发。
- **onUpdated**: 视图已经根据新数据重新渲染了。
  - **注意**：避免在这里修改数据，否则会死循环。

## 4. 销毁阶段 (Unmounting)
当组件被移除（如路由跳转，或 v-if="false"）时触发。
- **onUnmounted**: **关键钩子**。
  - **必须做的事**：清理副作用！
    1. `clearInterval` (清除定时器)。
    2. `window.removeEventListener` (移除全局监听)。
  - 如果不清理，这些后台任务会一直占用内存，导致 **内存泄漏 (Memory Leak)**。

## 总结图

```
[ Setup ] -> [ Compile Template ] -> [ onMounted (DOM Ready) ]
                                          |
                                    (Data Change)
                                          v
                                    [ onUpdated ]
                                          |
                                    (Component Remove)
                                          v
                                    [ onUnmounted (Cleanup) ]
```